import { Entity, Column } from 'typeorm';

/**
 * An entity class for items table in the database.
 */
@Entity('items')
export class ItemEntity {
	/**
	 * PK of the table.
	 */
	@Column({
		name: 'item_id',
		type: 'int',
		comment: 'PK of the table.',
		generated: 'increment',
		primary: true,
		nullable: false,
		unique: true
	})
	itemId: number;

	/**
	 * Unique code of the item.
	 */
	@Column({
		name: 'code',
		type: 'varchar',
		length: 50,
		comment: 'Unique code of the item.',
		nullable: false,
		unique: true
	})
	code: string;

	/**
	 * Name of the item.
	 */
	@Column({
		name: 'item_name',
		type: 'varchar',
		length: 100,
		comment: 'Name of the item.',
		nullable: false
	})
	itemName: string;

	/**
	 * Remarks / description of the item.
	 */
	@Column({
		name: 'remarks',
		type: 'varchar',
		length: 8000,
		comment: 'Name of the item.',
		nullable: true
	})
	remarks: string;

	/**
	 * User id of a user who created the record.
	 */
	@Column({
		name: 'createdby',
		type: 'int',
		comment: 'User id of a user who created the record.',
		nullable: false
	})
	createdBy: number;

	/**
	 * Date and time when the record is created.
	 */
	@Column({
		name: 'createddate',
		type: 'timestamp',
		comment: 'Date and time when the record is created.',
		nullable: false
	})
	createdDate: Date;

	/**
	 * User id of a user who modified the record.
	 */
	@Column({
		name: 'modifiedby',
		type: 'int',
		comment: 'User id of a user who modified the record.',
		nullable: true
	})
	modifiedBy: number;

	/**
	 * Date and time when the record is modified.
	 */
	@Column({
		name: 'modifieddate',
		type: 'timestamp',
		comment: 'Date and time when the record is modified.',
		nullable: true
	})
	modifiedDate: Date;

	/**
	 * Is generated by the system.
	 */
	@Column({
		name: 'is_active',
		type: 'boolean',
		comment: 'Is generated by the system.',
		nullable: false
	})
	isActive: boolean;
}
